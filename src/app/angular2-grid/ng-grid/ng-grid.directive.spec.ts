import { NgGridDirective } from './ng-grid.directive';

describe('NgGridDirective', () => {
  it('should create an instance', () => {
    // const directive = new NgGridDirective();
    // expect(directive).toBeTruthy();
  });
});
