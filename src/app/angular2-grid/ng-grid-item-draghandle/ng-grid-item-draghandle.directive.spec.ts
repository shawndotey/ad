import { NgGridItemDraghandleDirective } from './ng-grid-item-draghandle.directive';

describe('NgGridItemDraghandleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgGridItemDraghandleDirective();
    expect(directive).toBeTruthy();
  });
});
