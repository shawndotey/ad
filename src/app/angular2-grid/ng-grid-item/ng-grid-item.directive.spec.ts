import { NgGridItemDirective } from './ng-grid-item.directive';

describe('NgGridItemDirective', () => {
  it('should create an instance', () => {
    // const directive = new NgGridItemDirective();
    // expect(directive).toBeTruthy();
  });
});
