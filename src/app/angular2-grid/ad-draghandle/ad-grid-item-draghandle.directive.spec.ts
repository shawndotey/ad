import { AdDraghandleDirective } from './ad-draghandle.directive';

describe('AdGridItemDraghandleDirective', () => {
  it('should create an instance', () => {
    //const directive = new AdGridItemDraghandleDirective();
   // expect(directive).toBeTruthy();
  });
});
