import { AdGridDirective } from './ad-grid.directive';

describe('AdGridDirective', () => {
  it('should create an instance', () => {
    // const directive = new NgGridDirective();
    // expect(directive).toBeTruthy();
  });
});
