import { AdResizehandleDirective } from './ad-resizehandle.directive';

describe('AdResizehandleDirective', () => {
  it('should create an instance', () => {
    const directive = new AdResizehandleDirective();
    expect(directive).toBeTruthy();
  });
});
