import { AdGridItemDirective } from './ad-grid-item.directive';

describe('AdGridItemDirective', () => {
  it('should create an instance', () => {
    // const directive = new NgGridItemDirective();
    // expect(directive).toBeTruthy();
  });
});
